<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.samvankooten.finnstickers">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateHidden">

            <!--
            The above windowSoftInputMode line keeps the keyboard from auto-opening.It tries to
            do that becuase of the text input that allows GBoard to be opened for sticker testing
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="finnstickers"/>
                <data android:host="sticker"/>
            </intent-filter>
        </activity>

        <activity android:name=".ContentPickerPackPickerActivity"
                  android:label="Finn Stickers" >
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />
                <category android:name="android.intent.category.OPENABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/jpeg" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PICK" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/jpeg" />
            </intent-filter>
        </activity>

        <provider
            android:name=".StickerProvider"
            android:authorities="net.samvankooten.finnstickers.StickerProvider"
            android:exported="true"
            android:grantUriPermissions="true">
        </provider>

        <receiver
            android:name=".AppIndexingUpdateReceiver"
            android:exported="true"
            android:permission="com.google.android.gms.permission.APPINDEXING">
            <intent-filter>
                <action android:name="com.google.firebase.appindexing.UPDATE_INDEX"/>
            </intent-filter>
        </receiver>

        <!-- Grant the AppIndexingUpdateService permission and enable it to run after being triggered -->
        <!-- See https://firebase.google.com/docs/app-indexing/android/personal-content -->
        <!-- <uses-permission android:name="android.permission.WAKE_LOCK" />
        <service android:name=".AppIndexingUpdateService"
                 android:permission="android.permission.BIND_JOB_SERVICE" /> -->

        <receiver android:name=".OnUpdateReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name=".UpdateJob"
                 android:label="Sticker updater"
                 android:permission="android.permission.BIND_JOB_SERVICE"
                 />

        <activity
            android:name=".StickerPackViewerActivity"
            android:label="@string/title_activity_sticker_pack_viewer"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.samvankooten.finnstickers.MainActivity"/>
        </activity>
    </application>

</manifest>