<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Saved photos are stored in getExternalFilesDir(Environment.DIRECTORY_PICTURES), and I'm
    not sure how to reference that here in xml -->
    <external-path name="ar_images" path="." />
    <cache-path name="shared" path="shared/" />
</paths>